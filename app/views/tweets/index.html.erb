<h1>ツイート一覧</h1>
<table>
 <tr>
  <th>投稿内容</th>
 </tr>
<% @tweets.each do |tweet| %>
 <tr>
  <td><%= tweet.content %></td>
  <td><%= link_to '詳細を確認する', tweet_path(tweet.id) %></td>
  <td><%= link_to "投稿を編集する", edit_tweet_path(tweet.id), data: { confirm: '本当に編集して良いですか?'} %></td>
  <td><%= link_to '投稿を削除する', tweet_path(tweet.id), method: :delete, data: { confirm: '本当に削除して良いですか?'} %></td>
  </tr>
<% end %>
</table>
<br>
<%= link_to '新しくツイートする', new_tweet_path %>